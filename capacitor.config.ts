import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic123456.starter',
  appName: 'propertydek-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
