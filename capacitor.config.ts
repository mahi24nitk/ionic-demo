import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic12345.starter',
  appName: 'propertydek-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
